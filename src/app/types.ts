export type MarketCapPoint = {
  ticker: string;
  name: string;
  date: string;
  market_cap: number;
  rank: number;
};

export type TodayRow = {
  ticker: string;
  name: string;
  market_cap: number;
  rank: number;
};
